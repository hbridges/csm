

<!-- Modal -->
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title" id="myModalLabel">Add a comment</h4>
      </div>
      <%= simple_form_for @photo, url: album_photos_path(@album) do |f| %>
        <div class="modal-body">
          <%= f.input :picture %>
          <%= f.input :name %>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
          <%= f.submit "Add Photo", class: 'btn btn-primary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>





  <% if admin_signed_in? %>
    <div class="">
      <%= link_to 'Edit Album', edit_album_path(@album), class: 'btn btn-primary' %>
      <%= link_to 'Destroy Album', album_path(@album), method: :delete, data: {confirm: 'Are you sure you want to delete this?'},class: 'btn btn-danger' %>
  <br />
  <br />

      <!-- Button trigger modal -->
      <button class="btn btn-primary btn-lg" data-toggle="modal" data-target="#myModal">
        Add a photo
      </button>
    </div>
  <% end %>
  <br />

  <div class="row">
    <div class="booyah-box col-xs-6 col-xs-offset-3">
      <h1 class="album-h1"><%= @album.name %></h1>
      <br/><br/>
      <%= image_tag @album.picture, :class => 'photo img-responsive center-block' %>
      <div class="caption text-center">
        <h3><%= @album.description %></h3>
      </div>
      <br />
      <br />
    </div>
  </div>

<div class="row">
  	<% @album.photos.each do |photo| %>
  		<div class="booyah-box col-sm-6 col-md-4">
  		  <%= image_tag photo.picture, :class => 'photo img-responsive center-block' %>
  		  <br />
        <div class="caption text-center">
  		    <h3><%= photo.name %></h3>
        </div>
  			 <div class="pull-right">
  	   			 <%#= link_to 'Destroy', place_photo_path(@place), :method => :delete, :data => {:confirm => 'Are you sure you want to delete this?'}, :class => 'btn btn-danger' %>
  	 		 </div>
  	 	  <br />
      	 </div>
  	<% end %>
</div>
